<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.valentelmadafaka.pokemonapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/pokeballicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pokeballicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QuizzActivity"></activity>
        <activity android:name=".Pokemon_Info" />
        <activity android:name=".FichaActivity" />
        <activity android:name=".PokedexActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>